import { Card } from '../ui/card';
import { Badge } from '../ui/badge';
const features=[{title:'Revenue Risk Score',desc:'One metric that warns you before pipeline gaps and invoice drift become income volatility.'},{title:'Weekly Reset (7 minutes)',desc:'A guided flow that locks billable time, pipeline minimums, and deliverable priorities.'},{title:'Recovery Mode',desc:'Missed a week? Get a 48-hour rescue plan without starting over.'},{title:'Week Cards',desc:'An operating archive — the system gets harder to replace as you use it.'}];
export default function FeatureGrid(){return <section className='mx-auto max-w-6xl px-4 pb-8'><div className='mb-8'><Badge>Why it works</Badge><h2 className='mt-3 text-3xl font-semibold tracking-tight'>Not a planner. An operating system.</h2><p className='mt-2 text-white/70'>Delivery, pipeline, and invoices are the real drivers of stability.</p></div><div className='grid gap-4 md:grid-cols-2'>{features.map(f=>(<Card key={f.title}><h3 className='text-lg font-semibold'>{f.title}</h3><p className='mt-2 text-white/70'>{f.desc}</p></Card>))}</div></section>}
